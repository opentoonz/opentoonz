# Legacy compatibility file (do not add targets here)
message(STATUS "Legacy toonz compatibility directory present — sources have moved to 'flare/'.")
# Intentionally empty: this file exists so that scripts that expect a top-level
# `toonz` directory do not fail immediately. Please reference `flare/` for
# current sources and build logic.
